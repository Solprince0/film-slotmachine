import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import FilmIdeenSlotMachine from './FilmIdeenSlotMachine';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <FilmIdeenSlotMachine />
  </React.StrictMode>,
);